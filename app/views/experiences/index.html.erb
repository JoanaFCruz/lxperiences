<h1>Discover our LXperiences</h1>
<p>The app to discover the most fun part of Lisbon and regions</p>

<div>
  <% @experiences.each do |experience|%>
    <ul>
      <li><strong><%= link_to experience.title, experience_path(experience) %></strong></li>
      <li><strong></strong><%= experience.location %></li>
      <li><strong></strong><%= experience.price %> eur</li>
      <br>
    </ul>
  <% end %>
</div>

<br>

<div>
  <%= link_to "Add new experience", new_experience_path if policy(Experience).new? %>
</div>

<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
