<div class='page-top-margin'>
  <h1>Our offers</h1>
    <% @experiences.each do |experience|%>
      <ul>
        <li><strong><%= experience.title %></strong></li>
        <li><strong></strong><%= experience.location %></li>
        <li><strong></strong><%= experience.price %></li>
        <br>
      </ul>
    <% end %>

  <%= link_to "New experience", new_experience_path %>
</div>
